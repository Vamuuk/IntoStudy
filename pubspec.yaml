name: into_study
description: A Flutter application for students to share notes, ask questions, and collaborate.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # === НАЧАЛО: ДОБАВЛЕНЫ ПАКЕТЫ FIREBASE ===
  firebase_core: ^2.24.2 # Используйте последнюю стабильную версию
  cloud_firestore: ^4.13.4 # Используйте последнюю стабильную версию
  # === КОНЕЦ: ДОБАВЛЕНЫ ПАКЕТЫ FIREBASE ===

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true